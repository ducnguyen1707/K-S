1. Create IAM User "developer"
         ↓
2. Create IAM Policy (eks:DescribeCluster, eks:ListClusters)
         ↓
3. Attach Policy to User (developer can now view EKS clusters)
         ↓
4. Create Access Entry (developer joins Kubernetes group "my-viewer")
         ↓
Result: Developer can view cluster info + access Kubernetes resources